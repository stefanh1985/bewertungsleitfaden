
function copyPageLink(button){const url=window.location.href;if(navigator.clipboard){navigator.clipboard.writeText(url).then(()=>flash(button,'Link kopiert'));}else{window.prompt('Link kopieren:',url);}}
function flash(button,text){const old=button.textContent;button.textContent=text;setTimeout(()=>button.textContent=old,1600);}
function downloadPageAsWord(){const source=document.querySelector('.document-content')||document.querySelector('main');const title=document.querySelector('h1')?.textContent||document.title;const css='body{font-family:Arial,sans-serif;line-height:1.5}table{border-collapse:collapse;width:100%}th,td{border:1px solid #777;padding:6px;vertical-align:top}img{max-width:100%;height:auto}h1,h2,h3{color:#244b79}';const doc=`<!doctype html><html><head><meta charset="utf-8"><style>${css}</style></head><body><h1>${escapeHtml(title)}</h1>${source.innerHTML}</body></html>`;const blob=new Blob(['\ufeff',doc],{type:'application/msword'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=slugify(title)+'.doc';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(a.href),500);}
function escapeHtml(s){return s.replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}
function slugify(s){return s.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')||'bewertungsleitfaden';}
function filterFormats(){const q=(document.getElementById('formatSearch')?.value||'').toLowerCase().trim();document.querySelectorAll('.format-card').forEach(a=>{a.style.display=a.textContent.toLowerCase().includes(q)?'block':'none';});}
